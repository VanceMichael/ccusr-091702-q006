[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "cultural-schedule"
version = "0.1.0"
description = "文化空间联动排期台后端服务"
requires-python = ">=3.12"

[tool.setuptools]
py-modules = ["app", "migrate"]