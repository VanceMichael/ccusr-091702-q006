# 领域约定

文化空间联动排期台围绕跨场地节目线路、共享资源和变更通知保存业务事实。主要对象为节目版本、场地时窗、共享资源、转场约束、审批、线路、观众批次、通知。

必须长期遵守以下约束：正式锁定前同时校验全部时窗和资源；规则快照随发布保存；突发关闭只生成候选调整；候选经人工确认后才能替换线路。

时间以带时区的 ISO 8601 字符串交换，服务内部保存 UTC；外部写请求使用调用方提供的幂等键；审计记录采用追加方式保存，不通过覆盖来隐藏历史。